
  # webordable

  This is a code bundle for webordable. The original project is available at https://www.figma.com/design/NqeKy5S9rgue6p1E1cbPPZ/webordable.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  